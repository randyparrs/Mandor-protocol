import { LazyHardhatViemAssertions } from "../lazy-viem-assertions.js";
export default async () => {
    const handlers = {
        async newConnection(context, next) {
            const connection = await next(context);
            connection.viem.assertions = new LazyHardhatViemAssertions(connection.viem);
            return connection;
        },
    };
    return handlers;
};
//# sourceMappingURL=network.js.map